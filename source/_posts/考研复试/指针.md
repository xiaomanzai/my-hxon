#### 空指针

1. 用于初始化
2. 不可访问（0~255）

#### 野指针

指向非法的指针

（都不是我们可以指向的指针）

#### const修饰指针

指针常量
` const int *p = &a  `  指针不可变，指针所指向的值可变
常量指针 
`int * const p = &b`    指针可变，指针的值不可变

变量指针都不可改
`const int * const p = &a `


#### 指针和数组

利用指针访问数组