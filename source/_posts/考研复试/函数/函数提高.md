#### 函数的默认参数

如果声明有了，实现就不用在写了 

后面的参数也需要默认参数

#### 函数的占位的参数

``` c++
void func(int a, int)

 func (10,10)// 没有10会报错
```
 可以赋予默认参数

#### 函数重载

函数名重载，函数名相同

满足条件
1. 同一个作用域
2. 函数名相同
3. 函数的参数不完全相同

尽量不要使用默认参数