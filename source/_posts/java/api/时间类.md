---
title: 时间类
category: java api
tags:
  - java
---

## date

成员变量

`private long time` 表示毫秒数

空参构造

获取 `system.currentTimeMillis()`
之后再传给本类构造

方法

set 和 get 方法

## simpleDateFormat

作用
1. 格式化
2. 解析

构造方法
1. 带参 string pattern

方法
1. format
把日期对象改成字符串

2. parse 
把字符串改成日期对象, 可以通过 get time 方法获得毫秒值

pattern 模式
y 年  M 月 d 日 H 时 m 分 s 秒 EE 星期 

## Calendar

方法
 
`calendar getInstance（）` 获取当前时间的日历对象

获取当前时区的信息并放到一个数组中

方法
``` java
date getTime () 
void setTime (Date date)
long getTimeInMillis()
void setTimeInMillis(long millis)
int get(intt field) 获取日历中某个字段信息
void set(int field,int value) 修改某个字段的信息
void add(int field,int amount) 为某个字段增加/减少指定的值
```


# jdk 8 中的时间

## date 类

ZoneId 时区
Instant  时间戳
ZoneDateTime 带时区的时间

#### zoneid

method

```java
static set<string> getAvaliableZoneIds () 获取 java 所有支持的时区
static ZoneId systemDefault () 默认
static zoneId of (string zoneid) 指定一个时区
```

#### instant

``` java
static Instant now () 标准时间

static instant ofXxxx (long epochMilli) 

ZonedDateTime atZone (zoneid zone) 指定时区

boolean isxxx (instant otherinstant) before or after

instant minusxxx (long millis)
instant plusxxx (long millis)
```

#### zonedatetime

``` java
static zonedatetime now ()

static zonedatetime ofxxx () 指定时间

zonedatetime withxxx() 修改时间

zonedatetime minusxxx()
zonedatetime plusxxx()
```


#### datetimeformatter

``` java
static datetimeformatter ofmatter("string")

string format(datetime)
```


# calender & utils

#### localdate, localtime ,localdatetime

method:

``` java
static xxx now() xxx ->(ocaldate, localtime ,localdatetime)
static xxx of()
get()

isbefore or after

with() update time
minus()
plus()

localdate tolocaldate()
localtime tolcaltime()

```

#### duration, period, chronounit

static period between (xxx, xxx) 间隔对象

xxx (时间对象)  get

chronounit 是最为全面的 

static xxx (time pojo) between (xxx, xxx)
需要指定 year ，week 来先获取 chronounit 对象

前面的都是直接通过 between 获取就行，但是需要用 get 获取

